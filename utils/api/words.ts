import { AxiosInstance } from "axios";

export const WordsApi = (instance: AxiosInstance) => ({
  // async getAll(): Promise<Words[]> {
  //   const { data } = await instance.get<ApiRequestWords>("/words");
  //   return data.teams;
  // },
});
